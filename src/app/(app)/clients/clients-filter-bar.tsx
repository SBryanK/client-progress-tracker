// DEPRECATED — no longer imported. Kept as an empty shim to avoid breaking
// any stale references. The `/clients` page now uses pill-tab bucket nav
// directly. Safe to remove when convenient.
export {};
